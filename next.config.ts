import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  //   async redirects() {
  //   return [
  //     {
  //       source: '/:path((?!api/).*)', // Corresponde a TUDO, exceto a página inicial e rotas de API
  //       destination: '/', // Redireciona para a página inicial
  //       permanent: false, // Use 'false' pois é um redirecionamento temporário
  //     },
  //   ]
  // },
};

export default nextConfig;
